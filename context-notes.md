# 개발 컨텍스트 및 결정 의사록

개발 중에 내린 결정과 그 배경 논리를 기록하는 문서입니다.

## 결정 사항

### React Native Expo 프레임워크 선정

- 네이티브 모듈 설정이 간단하고 로컬에서 즉시 시뮬레이션 및 웹 테스트가 가능한 Expo 프레임워크를 선택하였습니다.
- TypeScript를 도입하여 API 데이터 모델링의 안전성을 극대화합니다.

### 스타일링 방식에 대한 논의

- 복잡한 NativeWind 빌드 설정 대신 안정성과 커스텀 편의성을 우선하여 표준 React Native StyleSheet를 사용해 정교한 Pearl White 테마의 디자인을 구축합니다.

### 로컬 Uvicorn 통신 IP 설정 방식

- 시뮬레이터와 물리 기기에서의 연결성을 위해 앱 실행 시 기본 API 주소를 유연하게 전환할 수 있도록 처리합니다.

### Firebase 연동 프로젝트 정보 설정

- 사용자가 제공한 실제 프로젝트 ID인 action-b8c75를 설정 파일에 반영하여 실질적인 데이터 동기화 기반을 마련하였습니다.

### UI 여백 축소 및 모바일 시군구 필터 전국 확장

- PC 대시보드의 글꼴 배율 축소(120% -> 100%) 및 주요 패널(서랍, 사이드바 등)의 padding/margin 축소를 통해 빈 공간이 보이지 않도록 UI 밀도를 조절하였습니다.
- 모바일 앱의 시도 필터 대상을 5개에서 17개 전국 단위로 확장하고, 칩 컴포넌트들이 영역을 과도하게 차지하지 않도록 ScrollView horizontal 기반 가로 스크롤 레이아웃을 도입하였습니다.

### 온라인 연동 검증 및 아키텍처 문서화 방향 결정 (2026-06-06)

- PC 대시보드(index.html)와 모바일 앱은 Firestore 연동을 통해 100% 온라인 직접 조회가 정상적으로 수행됨을 확인하였습니다.
- 그러나 신규 데이터를 주기적으로 수집하여 Firestore로 마이그레이션하는 크롤러 및 DB 엔진(Python, SQLite)의 로컬 보존이 필요한 상태이므로, 이에 대한 사용자 피드백을 수렴하여 삭제 범위를 결정할 예정입니다.
- 시스템 전체의 구조와 데이터 연동 플랜을 정리하는 `architecture.md` 문서를 한국어로 작성하기로 결정하였습니다.

### 데이터 수집 범위 확장 및 수집량 증폭 결정 (2026-06-06)

- 수집되는 경매 자산 데이터의 수가 너무 적은 원인을 분석한 결과, `court_scraper.py` 내부의 법원 수(10개), 기일 세션 수(15개), 검색 달(2개월)이 하드코딩으로 강하게 제한되어 있었음을 식별하였습니다.
- 이를 해결하기 위해 법원 전수 조사, 3달치 수집, 기일 세션 제한 전면 해제를 적용하여 데이터 수집량을 대폭 증대시키기로 결정하였습니다.
- 대법원 서버 차단 감지를 피하기 위해 요청 딜레이를 무작위로 유지하며 안정성을 확보하기로 결정하였습니다.

### 대시보드 탭 전환 시 화면 깨짐 및 페이지 누락 패치 결정 (2026-06-06)

- 탭 버튼 클릭 시 기존에 설정되어 있던 Tailwind 반응형 글자 크기 및 정렬 클래스가 `className` 재할당으로 인해 완전히 덮어써지면서 UI 찌그러짐 현상이 발생한 것을 식별하였습니다. 이를 보완하기 위해 `classList` API를 활용하여 활성/비활성 관련 테두리 및 텍스트 색상 클래스만 부분 변경하도록 결정하였습니다.
- 브라우저 해상도 변경 시 가동되는 `initResponsiveLayout` 함수가 메인 뷰들의 `className`을 재설정하면서, 탭 전환으로 부여된 `hidden` 클래스가 초기화되어 뷰들이 상호 충돌하여 화면에서 유실되거나 중첩 노출되는 문제를 확인하였습니다. 이를 해소하고자 전역 변수 `currentTabId`를 선언하고, 레이아웃 변경 직후 현재 탭 상태에 맞춰 각 뷰의 `hidden` 상태를 강제로 동기화하도록 구현하였습니다.

### 모바일 폰트 시인성 개선 및 Firestore Quota 초과 대처 (2026-06-06)

- 모바일 웹앱에서 필터 내부 글자 크기가 작다는 사용자 의견을 반영하여 `FeedScreen.tsx` 의 필터 패널 내 폰트 크기 설정을 2~3px 가량 전면 상향 조정하기로 하였습니다.
- 서버에 1,895건의 데이터가 있음에도 화면에 3개만 보인 원인을 추적한 결과, 무료 Firestore의 일일 읽기 한도(50,000 reads)가 소진되면서 Firestore SDK에서 `RESOURCE_EXHAUSTED` Quota 초과 에러가 발생한 것을 규명하였습니다.
- Firestore 로드 실패 시 예비용 하드코딩 데이터(`fallbackData` 4건 중 마감 필터로 3건 노출)만 반환되던 문제를 방어하기 위해, 데이터 로드 성공 시 브라우저 `localStorage`에 데이터를 캐싱해두고, 에러 시 이 캐시를 복구하여 1,895건의 실데이터를 무중단으로 볼 수 있도록 캐싱 전략을 결정하였습니다.

### 헤더 반응형 찌그러짐 및 계산기 키패드 시인성 개선 결정 (2026-06-08)

- 화면 폭이 좁은 모바일 뷰에서 헤더의 연동 센서 텍스트("실시간 데이터 연동 정상 (N건)")가 공간 부족으로 찌그러지고 위로 솟구치는 문제를 해결하기 위해, CSS 미디어 쿼리와 JS 갱신 부분에 반응형 클래스(`hidden sm:inline` 및 `inline sm:hidden`)를 도입하여 모바일에서는 "🟢 N건" 형태로 간소화해 노출하기로 하였습니다.
- 스마트 소요자금 계획서 가상 키패드의 전체 레이아웃 규격을 유지하면서 숫자 폰트 크기만 선택적으로 확대하기 위해, 숫자 버튼(클래스 `font-outfit`이 부여된 버튼들)에 한하여 폰트 크기를 데스크톱 기준 `16px`, 모바일 미디어 쿼리 기준 `18px`로 상향 적용하기로 결정하였습니다.

### 소셜 로그인 예외 처리 및 안정화 결정 (2026-06-08)

- Supabase Auth에서 공식적으로 지원하지 않는 Naver 소셜 로그인 시도 시, 사용자에게 "네이버 로그인은 준비 중"이라는 안내 메시지를 팝업으로 노출하고 차단하는 방식을 택하였습니다.
- 구글 및 카카오 로그인 시 redirect URL을 쿼리 스트링이 제거된 순수 도메인 주소로 명시하여 Supabase Auth 리다이렉션 예외를 방지하도록 조치하기로 결정하였습니다.

### 모바일 상세 화면 (DetailScreen.tsx) 필수 기능 연동 결정 (2026-06-08)

- PC 대시보드 웹에만 존재하던 '입찰 당일 필수 체크리스트'를 모바일 앱에도 탑재하기 위해, 매물의 출처(`court` 혹은 기타)에 따라 법원경매용 및 온비드 공매용 체크리스트를 동적으로 구분하여 렌더링하기로 결정하였습니다.
- '정부 실무 공용 정보망 연동 및 서류 조회' 버튼 그리드를 모바일 상세 뷰 최하단에 구현하며, 네이버 지도 및 네이버 부동산 검색 연동 시 해당 매물의 실제 주소를 인코딩하여 즉시 실거래가 및 위치를 조회할 수 있게 연동성을 극대화하기로 결정하였습니다.
- 모바일 앱의 가상 키패드 역시 숫자 글자 크기를 18에서 22로 상향하여 가독성을 개선하기로 하였습니다.

### Supabase OAuth "Unsupported provider" 오류 방지 및 사용자 가이드 추가 (2026-06-08)

- 구글 소셜 로그인 시 발생하는 Supabase 서버 측 제공자 미활성화(Disabled) 에러에 유연하게 대처하기 위해, 에러 발생 시 단순 에러 내용 대신 "Supabase 대시보드에서 Google/Kakao 제공자를 먼저 활성화해야 한다"는 조치 방법을 명확하게 알려주는 안내 팝업을 추가하기로 하였습니다.
- '사용자 가이드' 탭 내부에 Supabase OAuth 설정 방법 및 이메일 가입 시 메일 인증을 건너뛰는 팁(Confirm email 설정 끄기)을 포함한 상세 서버 콘솔 가이드를 추가하여, 사용자 `<hl1oex@gmail.com>` 등의 가입 테스트를 용이하게 하도록 유도하기로 결정하였습니다.

### 구글 소셜 로그인 리다이렉션 해결 및 로그인 성공 시 즐겨찾기 자동 전환 결정 (2026-06-08)

- 구글 소셜 로그인 성공 후 localhost:3000으로 튕기는 문제는 Supabase 서버 측의 URL Configuration 설정에서 허용 목록에 배포 주소가 빠져있기 때문에 발생하는 현상임을 규명하였습니다.
- 이를 해결하기 위해, 사용자가 대시보드에서 수정해야 할 가이드를 index.html 및 모바일 앱의 GuideScreen.tsx에 추가하였습니다.
- 또한, 로그인 완료 시 자동으로 관심 목록을 보게 해달라는 요구사항을 반영하였습니다.
  - 웹 대시보드(index.html)에서는 onAuthStateChange 콜백에서 SIGNED_IN 이벤트 발생 시 관심 매물 보기 필터(showFavoritesOnly = true)를 자동 가동하도록 연동하였습니다.
  - 모바일 앱(App.tsx)에서는 AuthScreen 로그인 성공 시 activeTab을 'favorites'로 변경하여 관심 목록 탭으로 자동 전환 처리하였습니다.

### 회원가입 창 UI 개선 및 소셜로그인 리다이렉트 폴백 적용 결정 (2026-06-08)

- 회원가입 모드일 때 이메일 비밀번호 입력 외에 비밀번호 오타 방지를 위해 '비밀번호 확인' 입력 필드를 웹(index.html)과 모바일 앱(AuthScreen.tsx)에 각각 추가하였습니다.
- 가입 모드 제출 시 두 비밀번호의 일치 여부를 대조 검증하는 클라이언트 로직을 추가하여 안전성을 강화하였습니다.
- 회원가입 모드 상태에서는 하단의 간편 소셜 로그인 버튼 라벨 텍스트도 동적으로 'XX 계정으로 회원가입'으로 가변 노출되도록 하여 프리미엄 일체형 디자인을 구축하였습니다.
- 사용자가 로컬 파일 경로(file://)에서 열었거나 부정확한 환경에 있을 경우 소셜 로그인 완료 후 리다이렉트 도중 튕겨서 에러가 나는 문제를 방지하기 위해, 웹과 모바일 앱에서 OAuth 호출 시 리다이렉트 주소(redirectTo)를 안전한 배포 호스팅 서버 주소(<https://action-b8c75.web.app/>)로 항상 안전 수렴하도록 폴백 연동하였습니다.

### 소셜 로그인 사전 구성(Authentication -> URL Configuration) 경고 팝업 추가 결정 (2026-06-08)

- 구글 소셜 로그인 성공 직후 localhost:3000으로의 리다이렉트 접속 불가능 문제를 해결하기 위해서는, 클라이언트 코드단뿐만 아니라 Supabase 서버 측 콘솔에서 배포 도메인을 등록하는 필수 사전 절차가 필요합니다.
- 사용자가 이 사실을 쉽게 자각하고 자가 조치를 취할 수 있도록, 웹(index.html)과 모바일 앱(AuthScreen.tsx)의 소셜 로그인 버튼을 클릭했을 때 Supabase URL Configuration 설정 등록 여부를 묻는 사전 확인 대화상자(Confirm 및 Alert)를 강제 팝업하도록 조치하였습니다.
- 해당 팝업에서 사용자가 예를 클릭해야만 로그인이 속행되며, 등록되지 않았을 경우 사전에 차단되어 localhost:3000으로의 오작동 및 튕김 현상을 완벽하게 방지하였습니다.

### 소셜 로그인 사전 구성 확인 팝업 제거 결정 (2026-06-09)

- Supabase 대시보드 내의 Site URL 및 Redirect URLs 설정이 전량 올바르게 동기화 완료되어 정상 동작함을 직접 검증하였으므로, 사용을 방해하는 기존 사전 확인용 대화상자(confirm 및 Alert)를 웹과 모바일 앱 전체에서 완전 삭제 처리하였습니다.

### 프리미엄 파비콘 시안 B (진한 투명 AI 테크) 최종 채택 및 이식 결정 (2026-06-09)

- 사용자의 피드백에 따라 '미래형 AI 테크' 시안 B를 최종 파비콘으로 채택하였습니다.
- 브라우저 탭 및 다양한 OS 다크/라이트 모드에서 심볼 아이콘이 명확하게 렌더링되도록 배경을 완전한 투명(transparent)으로 보정하고, 색상 대비와 선의 두께를 한층 짙고 선명하게 다듬은 '진한 투명 AI 테크' 버전을 생성하여 루트 및 배포 폴더에 정식 이식하였습니다.

### 로그인 성공 시 관심 목록 강제 필터링 및 탭 이동 제거 (2026-06-09)

- 로그인 완료 직후 사용자의 관심 매물 내역이 비어 있을 경우(0건), 화면이 완전히 텅 비어 보이는 버그성 사용성을 개선하기 위해 로그인 완료 즉시 관심 목록 필터를 강제 활성화하거나(index.html) 관심목록 탭으로 강제 이동(App.tsx)하던 자동 로직을 제거하였습니다.
- 대신, 로그인한 유저에 한해 메인 추천 매물 피드 상단의 '정렬 옵션' 바 우측에 실시간으로 관심 목록 개수를 연동해 보여주는 프리미엄 골드/앰버 톤의 '내 관심 매물' 필터 버튼을 추가하여 필요 시 간편하게 모아볼 수 있는 주도적 UX를 설계하였습니다.

### 모바일-PC 파비콘 불일치 원인 분석 및 원형 파비콘(3D 네온 글래스모피즘) 최종 배포 결정 (2026-06-09)

- PC 대시보드는 루트의 `favicon.png`를 직접 읽는 반면, 모바일 웹 앱은 `mobile-app/assets/favicon.png`를 참조하여 빌드 프로세스 중에 `dist/mobile/favicon.ico`로 변환 배포되고 있었습니다. 모바일 폴더의 자산이 과거 파일로 방치되어 불일치가 발생한 문제를 확인하였습니다.
- 사용자의 선호에 맞춰 동그라미 형태에 높은 시인성을 갖춘 '3D 네온 글래스모피즘 테크(시안 B)'를 최종 파비콘으로 이식 및 동기화하였고, Expo Metro 번들 재빌드 후 배포 완료하였습니다.

### 상세 분석 뷰 내 '종합 투자 가치 및 규제/구조 상세 정보' 신설 (2026-06-09)

- 사용자의 요구사항을 수렴하여 PC 웹 대시보드(index.html)와 모바일 Expo 앱(DetailScreen.tsx) 상세 분석 뷰에 실무 중심의 고부가 가치 분석 카드 2종을 새로 이식하였습니다.
- **종합 투자 가치 및 규제 정보 리포트**: 자산의 성격 및 등급 점수에 기반한 투자용/실거주용 구분 및 설명 제공, 말소기준권리 기준 전후 말소확인여부 연산 제공, 대항력 유무 및 보증금 인수 주의 배지 탑재, 주소지에 연동된 2주택 취득세 규제 지역 여부 자동 판별 제공, 캠코 공매 시 토지거래허가 의무 대상 및 전세 갭투자 가능 여부 자동 판별을 탑재하였습니다.
- **물건 상태 진단 및 평면 구조 도면 갤러리**: AI 적합 점수를 연동한 건물 외관 상태 진단 프로그레스 바 제공, 매물 타입별(아파트/오피스텔/상가 등) 가상 평면 구조(3-Bay 등) 추정 배지 및 건축물대장 현황도면 무료 발급(정부24)을 위한 온라인 아웃링크를 이식하였습니다.

### 대항력(대항권) 여부 정밀 진단 규칙 고도화 (2026-06-09)

- 사용자의 대항력(대항권) 추가 정밀 분석 요구를 반영하여, 기존 텍스트 기반 대항력 판단 로직을 세분화하여 4가지 분류로 고도화하였습니다.
- 첫째, 대항력 없음(안전)은 임차인이 없거나 대항력이 소멸되는 안전한 상태를 의미합니다.
- 둘째, 대항력 포기(안전)는 HUG 등 채권승계인의 대항력 포기 조건부 매각 건으로, 인수 금액이 발생하지 않는 안전한 상태를 뜻합니다.
- 셋째, 대항력 미상(확인 요망)은 임차인이나 전입세대는 존재하나 권리 정보가 불투명하여 현장 조사가 요망되는 상태를 뜻합니다.
- 넷째, 선순위 대항력 주의는 말소기준권리보다 빠른 선순위 임차인이 존재하여 보증금 인수 리스크가 확실한 위험 상태를 가리킵니다.
- 웹 대시보드(index.html)와 모바일 앱(DetailScreen.tsx)에 해당 규칙을 동시 적용하고, 대항력 미상 시 경고 뱃지 색상을 호박색(amber)으로 구분 노출되도록 뱃지 바인딩 스타일을 수정하여 프리미엄 권리진단 직관성을 향상시켰습니다.

### 자산 투자 성향(투자 가치형 / 실거주형) 구분 및 필터링 설계 (2026-06-09)

- 사용자의 경매 및 공매 매물 탐색을 고도화하기 위해 '투자형' 매물과 '실거주형' 매물을 직관적으로 분류하여 제공하기로 결정하였습니다.
- 분류 기준은 다음과 같습니다.
  - 상업용 부동산(상가/근린/상업/빌딩 등) 및 개발 투자성이 강한 토지/공장 등은 '투자형'으로 분류합니다.
  - 아파트/빌라/단독주택 등 주거용 부동산은 기본적으로 '실거주형'으로 분류합니다.
  - 단, 주거용 부동산 중 AI 권리진단 점수가 85점 이상인 A급 우량 매물은 '투자 & 실거주 겸용'으로 판별하여 양쪽 성향에 모두 매칭되며 뱃지도 동시에 노출되도록 처리합니다.
- 이에 따라 웹 대시보드와 모바일 앱에 투자 성향 필터(전체, 투자형, 실거주형)를 탑재하고 매물 카드 및 상세 뷰에 전용 뱃지(🏆 투자 추천, 🏠 실거주 추천)를 이식하기로 하였습니다.

### 주변시세 확인 기능(씨:리얼) 도입 및 모바일 앱 이식 결정 (2026-06-10)

- 상세 분석 화면에서 주변시세를 조회할 수 있도록 LH 씨:리얼 포털 연동을 구현하기로 하였습니다.
- 씨:리얼 종합검색 페이지의 구형 아웃링크 주소(`asstInfoList.do`)가 사이트 개편으로 404 오류를 뱉는 장애를 식별하여, 신규 메인 주소(`<https://seereal.lh.or.kr/>`)로 리다이렉트되도록 긴급 수정 조치하였습니다.
- 모바일 `DetailScreen.tsx`에 이 기능을 완벽 이식하였으며, 네이티브 앱 환경에서 발생할 수 있는 클립보드 복사 에러를 방어하고자 `expo-clipboard` 동적 로드(Lazy Load) 구문을 탑재하여 TypeScript 및 런타임 신뢰성을 모두 달성했습니다.
- 웹 버전 `index.html`에서 링크를 바인딩하는 과정 중에 발생하던 런타임 TypeError 문제를 원천 차단하기 위해 엄격한 Null Check 방어 코드를 적용하였습니다.

### 상세페이지 12단 재구성 및 검색 기능 고도화 결정 (2026-06-10)

- 리치고 경매 상세화면 구성과 일치하도록 종합 정보, 권리분석, 입찰분석, 예상배당표, 인수분석, 점유현황, 등기현황, 매각통계, 시세/실거래가, 단지정보, 지도, 내부 평면도 / 구조도 등 총 12개의 프리미엄 탭 메뉴로 상세페이지를 전면 개편하기로 결정하였습니다.
- 모바일 화면 가로 공간 제약을 극복하기 위해 `ScrollView horizontal` 기반 가로 스크롤 탭 메뉴바를 모바일 앱에 구현하고, 기존 스마트 정밀 계산기와 서류조회 버튼들도 누락 없이 알맞은 탭 패널 내로 재배치하기로 하였습니다.
- 검색 필터 내에 "⚖️ 관할 법원 필터" 섹션을 추가하여 다중 선택 체크박스 방식으로 전국 법원 데이터를 일괄 필터링할 수 있는 다차원 조건식 검색 엔진을 구축하기로 결정하였습니다.
- 상세 조회 후 되돌아갈 때 검색 조건이 초기화(풀림)되는 불편을 방지하기 위해 웹은 `localStorage` 캐싱 및 `popstate` 브라우저 히스토리 제어 가드를 적용하고, 모바일 앱은 필터 상태를 최상위 `App.tsx` 글로벌 상태로 이관하여 컴포넌트 마운트 해제 시에도 데이터 유실을 완전 방지하기로 하였습니다.
- 프로그램의 공식 한글 명칭을 "부동산경공매 검색시스템"으로 일괄 변경하고 상단 제목 영역을 클릭할 시 전체 필터 초기화와 동시에 홈 피드로 복귀하는 주도적 인터랙션 경로를 설정하였습니다.
- 크롤링 프로세스 완료 후 성공 및 실패 세부 로그 통계를 "<hl1oex@gmail.com>" 이메일로 자동 전송하기 위해 `scrapers/run_daily_scrapers.py` 통합 러너를 신설하고 `psycopg2`와 `smtplib` 연동 파이프라인을 구축하기로 하였습니다.

### 상세페이지 미노출 버그 정밀 수정 및 React Native Modal 오작동 방어 (2026-06-10)

- PC 웹 대시보드에서 12단 프리미엄 탭을 포팅하는 과정에서 런타임 Null 에러를 발생시키던 누락된 DOM ID(detail-ai-*, detail-spec-official-* 등)들을 마크업 단에 모두 복구하여 상세 정보 서랍이 원활히 동작하도록 조치했습니다.
- 모바일 웹 및 하이브리드 앱 환경에서 React Native 표준 `Modal` 컴포넌트가 브라우저의 렌더링 컨텍스트(z-index 및 레이아웃 계층 구조 등)에 가려져 투명하게 먹통이 되거나 화면에 렌더링되지 않는 현상을 식별하였습니다.
- 이를 해결하기 위해 플랫폼별로 제약을 받는 `Modal` 대신, 뷰포트 전체를 차지하는 커스텀 절대 좌표 오버레이 뷰(Custom Absolute Overlay View) 구조로 상세화면 렌더링 방식을 개편하여 Web과 Native 앱 양쪽 모두에서 100% 화면에 안정적으로 렌더링되도록 하였습니다.

### 세분화 검색 필터, 네이버 지도 교체, 등급 업그레이드 및 광고 관리 고도화 결정 (2026-06-11)

- **물건 종류 7대 분류 세분화**: 기존 5개 카테고리에서 사용성 극대화를 위해 아파트, 오피스텔, 다세대/빌라, 단독/다가구, 상가/점포, 토지/임야, 공장/창고의 7가지 체크박스로 개편하여 매칭 알고리즘을 한층 더 정밀하게 다듬기로 결정하였습니다.
- **회원 등급 및 업그레이드 요청 기능**: `user_profiles`에 `upgrade_requested` 컬럼을 도입하여 고객이 대시보드 및 모바일 화면 우측 상단에서 자신의 등급을 실시간으로 확인하고 등급 상향을 요청하면 백엔드 어드민(`admin.html`)에서 즉시 확인하여 승인/조정할 수 있도록 관리 체계를 일원화하기로 하였습니다.
- **백엔드 광고 매니저 및 광고판 고도화**: 직접 유치형 배너 등록 시 실시간 규격 확인 가이드를 제공하고 애드센스 HTML 간 편리한 스위칭 버튼을 구축하여 사용자가 직관적으로 관리할 수 있도록 개선하였습니다.
- **면적 제곱미터-평수 병기**: 기본 매물 명세 정보에서 대지면적과 건물면적에 대해 `㎡`와 함께 `평` 단위를 동시 계산하여 병기 표기하도록 결정하였습니다.
- **지도의 네이버 지도 교체 및 레이어 분기**: 지도가 표기되는 공간을 네이버 지도 서비스 기반으로 교체하고 지번도, 위성지도 토글 버튼 클릭 시 네이버 지도의 적절한 지도 레이어로 이동하도록 링크를 분기 설계하였습니다.
- **유사 매물 거리 표기**: 추천되는 주변 3개 매물과의 거리를 미터(m) 단위로 동적 연산하여 "1000m 이내" 형태로 가독성 있게 나타내도록 수정하기로 결정하였습니다.
- **상세 뷰 5대 파트 타이포그래피 폴리싱**: 기본 명세, 소유자/채무자 정보, 기일 이력, 유사 추천 매물, 내부 평면도 파트의 글자 크기, 자간, 굵기 및 색상 조정을 거쳐 펄 화이트 테마에 어울리는 극상의 심미성을 완성하기로 결정하였습니다.

### Supabase Realtime 실시간 구독 제거 및 수동 새로고침 도입 결정 (2026-06-11)

- **성능 저하 원인 진단**: 경매 매물 정보는 스크레이퍼(크롤러)가 백그라운드에서 하루 단위로 대량 수집 및 갱신합니다. 기존에는 Supabase Realtime 채널을 구독하여 `properties` 테이블의 행이 1개라도 변경되면 클라이언트가 무조건 1000건씩 끊어서 전체 테이블을 다시 쿼리하고 UI를 리렌더링하게 설계되어 있었습니다. 스크레이퍼 작동 시간 동안 수천 건의 데이터가 갱신되면서 실시간 이벤트가 폭증하였고, 이에 따라 브라우저와 모바일 앱의 렌더링 스레드가 마비되어 사이트 속도가 극도로 느려지는 현상이 나타났습니다.
- **최적화 설계 및 해결책**: 실시간 웹소켓 구독(`.subscribe()`)을 전면 제거하고 수동 새로고침 방식을 채택합니다.
  - PC 웹 대시보드에서는 실시간 상태 배지 옆에 회전하는 새로고침 아이콘이 들어간 수동 새로고침 버튼을 배치하여 사용자가 원할 때 명시적으로 데이터를 새로 불러올 수 있게 합니다.
  - 모바일 앱에서는 `subscribeProperties` 및 `subscribeSyncStatus` 함수 내부의 Realtime 웹소켓 구독을 제거하고 최초 1회만 fetch 후 구독 해제 함수는 빈 콜백 `() => {}`을 리턴하도록 수정하여 기존 컴포넌트와의 호환성을 유지합니다.
  - 모바일 메인 피드(`FeedScreen.tsx`)의 `FlatList`에 당겨서 새로고침(Pull-to-Refresh) 기능을 추가해 모바일 환경에 최적화된 수동 갱신 수단을 제공합니다.
  - 데이터 캐싱 과정에서 발생하는 `JSON.stringify` 동기 처리 연산이 데이터 양이 비대할 때 UI 스레드를 멈추는 병목을 최소화하도록 예외 처리를 정돈합니다.

### 조건부 데이터 갱신 및 모바일 업데이트 알림 배지 도입 결정 (2026-06-11)

- **설계적 결정**: 사용자의 첫 접속 시 불필요한 Supabase 전체 조회를 방지하고 초기 속도를 극대화하기 위해 타임스탬프 기반의 조건부 데이터 동기화 기법을 이식합니다.
- **PC 웹 구현**: 페이지 로드 시 `sync_info` 테이블을 1회 쿼리하여 서버의 최신 크롤링 시간(`serverLastSync`)을 획득하고, 로컬 스토리지의 `cached_properties_timestamp`와 비교합니다. 로컬 캐시가 최신이면 전체 조회를 건너뛰고 캐시에서 바로 렌더링하며, 최신이 아닌 경우에만 자동으로 최신 데이터를 다운로드하여 성능을 개선합니다. 수동 새로고침 클릭 시에는 조건과 상관없이 무조건 갱신합니다.
- **모바일 앱 구현**: 모바일 앱 진입 시 마찬가지로 타임스탬프 비교를 수행하여 서버가 더 최신 상태일 때 메인 화면 상단에 "⚠️ 최신 경매 정보 업데이트 필요" 배지를 표시합니다. 사용자가 당겨서 새로고침(Pull-to-Refresh)을 직접 작동시켜 서버 데이터를 다운로드하면 배지를 숨기고 로컬 타임스탬프를 갱신하도록 구성하여 사용성 및 성능을 모두 강화합니다.

### 실제 면적 데이터 파싱 적재 우회 설계 및 단일 정렬 방식 개편 (2026-06-11)

- **실제 면적 데이터 미수집 및 추정치 고정 해소**: 대법원 경매 및 온비드 공매 크롤러가 실제 대지면적, 건물전용면적을 수집하지 않아 Supabase DB에 면적 정보가 없었고, 이로 인해 프론트엔드가 deterministic hash 알고리즘에 의존해 18.1평(59.9㎡)이나 25.6평(84.9㎡) 등으로 추정치를 고정 표출하던 한계를 식별하였습니다.
- **notes_content 메타데이터 인코딩 우회 기법 도입**: 원격 Supabase DB의 스키마를 직접 변경(컬럼 추가)하는 권한이 없으므로, 크롤러(`court_scraper.py`, `onbid_fetcher.py`)단에서 정규식 및 API 응답을 분석하여 실제 면적 정보를 파싱한 뒤, `notes_content` 텍스트 끝에 보이지 않는 메타데이터 블록(`\n\n__METADATA__:{...}__`) 형태로 인코딩하여 저장하기로 결정하였습니다. 프론트엔드에서는 로드 시점에 이 블록을 정규식으로 파싱하여 실제 면적 데이터로 가공 복원한 뒤 비고란의 원본 텍스트만 깔끔하게 보정하여 렌더링함으로써 DB 스키마 수정 없이 실제 데이터를 정상 매핑합니다.
- **단일 토글형 정렬 방식으로의 개편**: 기존 다중 정렬 체인(`activeSortChain`)은 `score`가 최우선으로 상시 누적 작동하여 금액순 정렬 등을 눌러도 정렬 기준이 정상 변경되지 않는 오작동 문제가 존재하였습니다. 이를 해소하기 위해 복잡한 체인 정렬을 철폐하고 사용자가 클릭한 키 기준 단일 정렬(동일 키 연속 클릭 시 오름차순/내림차순 토글) 방식으로 전환하여 금액순 정렬 등의 즉각적인 반응성을 실현하기로 결정하였습니다.
- **모바일 피드 정렬 칩 바 신설**: 웹뿐만 아니라 모바일 앱 `FeedScreen.tsx` 상단에도 5대 정렬 옵션 칩 바를 신설하여 사용자가 모바일에서도 손쉽게 점수순, 기일순, 금액순, 저감율순, 유찰순으로 정렬할 수 있게 개선하기로 하였습니다.

### 리치고 스타일 입지 및 금융 데이터 수집 고도화 방향 결정 (2026-06-12)

- **리치고 상세 벤치마킹**: 리치고(m.richgo.ai)의 아파트 경매 상세 정보 구조를 벤치마킹하여 우리 플랫폼에도 아파트 단지 정보(총 세대수, 준공년도, 건설사), 배정 초등학교 학군 정보, 최근 3개년 실거래가 리스트 및 동적 계산된 경락잔금대출 한도(LTV), 취득세 등의 금융 분석 정보를 융합 표출하기로 결정하였습니다.
- **수집의 기술적 한계 및 대안 우회 결정**:
  - KB 부동산 공식 시세 및 대법원 등기소 원본 무료 실시간 발급은 유료화 장벽과 법적 제약으로 인해 직접 크롤링 수집이 불가능합니다.
  - 대안으로 네이버 부동산 complexes 단지 시세/학군 API와 대법원 경매 매각물건명세서의 선/후순위 말소 권리 분석 테이블을 가동하여 등기 분석표로 완전히 대치하기로 하였습니다.
- **메타데이터 우회 적재 구조 확장**:
  - 새로 획득되는 대량의 고부가가치 아파트 단지/학군/실거래가 구조 데이터는 DB 컬럼 추가가 어려우므로, `notes_content` 맨 뒤의 비가시 메타데이터 블록에 JSON 필드를 추가 정의하여 포팅하는 구조적 결정을 내렸습니다.

### 정렬 기능 전면 리팩토링 및 단일 정렬 실제 이식 완료 (2026-06-12)

- **다차원 체인 정렬의 실제 폐지**: 기존에 기획은 하였으나 실제 구현 단계에서 잔재하고 있었던 다차원 누적 체인 정렬 로직(`activeSortChain`)을 완전히 들어냈습니다. 대신 단일 정렬 키(`currentSortKey`)와 방향(`currentSortDir`) 상태 변수 기반으로 전면 리팩토링하여 정렬 간섭 오작동을 근본적으로 제거하였습니다.
- **금액순 정렬의 직관적 기본 방향 개선**: 사용자가 '큰금액순' 정렬을 선호한다는 사실에 기초하여, 금액순(`price`), 저감율순(`discount`), 유찰순(`failed`)의 최초 클릭 시 정렬 방향을 오름차순(asc)에서 내림차순(desc)으로 정상 정립하였습니다.
- **모바일 수치 정렬 타입 디펜스 보강**: 모바일 앱 `FeedScreen.tsx`에서 최저입찰액(`minimum_bid`) 등의 값이 문자열이거나 undefined로 인해 정렬이 풀리는 버그를 사전에 방지하고자 안전한 형변환(`Number()`) 비교 로직을 보충 적용하였습니다.

### 온비드 비부동산 매물 수집 고도화 및 지능형 필터링 센터 오작동 수정 결정 (2026-06-12)

- **차량/동산 OpenAPI 신규 연동**: 온비드 수집기(`onbid_fetcher.py`)가 부동산 자산만 수집하던 제한을 해제하고, 차량 API 및 동산 API(유가증권, 기계장비, 기타물품 등)를 추가로 통합하여 수집 범위를 전면 확대하기로 결정하였습니다.
- **비부동산 데이터 표준화 및 소스 구분**: 차량/동산 매물은 `source` 필드에 `'onbid_etc'`로 표시하여 Supabase `properties` 테이블에 안전 적재하기로 하였습니다.
- **PC 웹 필터 캐시 버전업**: 지능형 필터링 센터가 캐시 데이터 충돌로 인해 신규 ptype(비부동산 등) 체크박스 상태를 강제 해제하던 문제를 해결하기 위해, 로컬 스토리지 캐시 버전을 `auction_filters_v3`로 상향하고 복원 예외 방어 코드를 적용하기로 결정하였습니다.
- **모바일 앱 필터 상태 보완**: 모바일 앱 `App.tsx` 내 필터 초기값 상태에 누락되었던 비부동산 `ptype` 항목들을 수동 추가하고, `FeedScreen.tsx`에서 온비드(`onbid`) 출처 체크 시 `onbid_etc` 매물도 함께 검색되도록 source 필터링 연동식을 보완하기로 결정하였습니다.

### 7대 상세페이지 중점 결함 수정 및 기능 개선 결정 (2026-06-12)

- **주소 및 제목 정제**: 괄호 내부 읍/면/동이나 아파트명 등이 일괄 소거되던 기존 정규식을 개선하여 주소지 및 명칭 등 핵심 지명 정보는 보존하면서 무의미한 용도 괄호만 정밀 소거하도록 정제 방식을 다듬기로 결정하였습니다.
- **실제 도면 부재 시 완전한 빈칸 처리**: 추정 도면의 임의 노출이나 도면 부재 안내 텍스트로 가득 차던 불필요함을 차단하고, 도면 에셋이 없을 경우에는 펄 화이트 스타일의 정숙한 빈 점선 상자 형태로 방치하고 모달 이벤트를 해제합니다.
- **실시간 임베드 지도 렌더링**: 구글 맵 Embed iframe API를 활용하여 웹 상세 탭 내에 아웃링크가 아닌 100% 라이브 지도 화면이 직접 노출되도록 내장형 시각화로 개편합니다.
- **대법원과 온비드 데이터 구분**: 모바일 앱 권리/입찰 탭 상단의 온비드 전용 고지 배너가 대법원 경매 매물일 때는 비활성화되도록 소스값을 기준으로 정확히 가드 분기합니다.
- **추정치 스타일 통일 및 꼬리표 명시**: 항목 전체를 노랗게 보이게 하던 그림자 효과와 진한 노란색 글씨(`estimatedText`) 및 보더를 완전히 걷어냅니다. 대신 진짜 추정치 값에만 "추정(허수)" 꼬리표를 텍스트 뒤에 덧붙여 가독성을 높입니다.
- **스마트 계획서 가로폭 단정화**: 3번 탭 전체 콘텐츠를 최대 720px로 정렬하여 브라우저 가로가 넓어지더라도 전체 카드가 동기화되어 깔끔하게 중앙 정렬되도록 레이아웃 정합성을 맞춥니다.
- **위치 & 시세분석 등급 블록 완전 해제**: PC 웹 버전의 상세Drawer에서 C등급 또는 B등급 유저가 접근할 때 4번 탭을 흐리게 블러 마스킹하고 결제 락을 걸던 차단 처리를 완전 우회/해제하여 누구나 100% 볼 수 있도록 정상화합니다.

### 상세페이지 4대 탭 디자인 통일 및 반응형 레이아웃 개편 결정 (2026-06-13)

- **디자인 통일 가이드라인**. 모든 카드를 `bg-white border border-slate-200 rounded-2xl p-4 shadow-sm`으로 일치시킵니다. 테이블 요소는 `div` 테이블 래퍼 컨테이너(`border border-slate-200 rounded-xl overflow-hidden mt-3 shadow-sm`)를 씌워 테두리를 둥글게 통일합니다.
- **가독성 향상**. 기존에 일부 10px, 11px 수준으로 지나치게 작아 읽기 어려웠던 텍스트 크기를 `text-[13px] sm:text-[14px]` 수준으로 대폭 키우고 헤더는 `bg-slate-100/80`에 굵게 표기하도록 정비합니다.
- **반응형 2열 및 1열 배치**. PC 버전에서는 화면 공간을 적극적으로 쓰기 위해 탭 1, 2, 3, 4 내부 요소들을 `grid grid-cols-1 lg:grid-cols-2 gap-4 sm:gap-5` 그리드 구조로 정렬합니다. 등기부 등본 요약 등 가로 넓이가 긴 정보 테이블은 단독 1열로 넓게 보여줍니다. 모바일은 사용자 편의성에 맞춰 1열 세로 스크롤 레이아웃과 가로 스크롤이 탑재된 테이블 패딩으로 조율합니다.
- **모바일 상세 화면(DetailScreen.tsx) 개편 및 E2E 검증 완수**.
  - `disclaimerText` 등 작았던 보조 텍스트 크기를 12px ~ 13.5px로 전격 인상하였습니다.
  - 비부동산(etc_specs) 탭의 테이블 찌그러짐을 없애고 부동산 명세와 동일한 세로 명세 보드로 통일 설계하였습니다.
  - 실서버 빌드 및 Firebase 배포 후 브라우저 서브에이전트 E2E 검증을 통과하였습니다.

### 모바일 앱 TypeScript 빌드 오류 디버깅 및 컴파일 안정화 (2026-06-13)

- **타입 정의 보완**. 모바일 앱 `types/index.ts` 내 `Property` 인터페이스의 `car_info` 에 `vin`, `engine_type`, `vehicle_type`, `base_location` 등 누락된 차량 정보 메타 속성을 명시적으로 정의하여 DetailScreen 내 속성 대입 에러를 해결하였습니다.
- **인자 매칭 및 변수 정의 누락 복구**. `DetailScreen.tsx` 의 `enrichPropertyDataMobile` 함수 내부에서 `textToSearch` 변수 선언이 누락되었던 문제를 복구하였습니다. 또한, `extractNonBuildingMetaMobile` 함수에 정의되지 않은 매개변수였던 `item.title`을 제거하고 2개의 인자(textToSearch, ptype)만 정상 전달하도록 수정하였습니다.
- **정규식 매치 인덱스 예외 가드**. `DetailScreen.tsx` 에서 정규식 `match` 실행 시 `index`가 `undefined`일 가능성에 대비하여 3항 연산자를 활용한 널 디펜스(null-safety) 코드를 추가하였습니다.
- **COLORS 속성 및 API 타입 검증**: `FeedScreen.tsx` 내 존재하지 않는 `COLORS.slate950` 색상 코드를 정상 탑재된 `COLORS.slate900`으로 변경하였습니다. 또한, `api.ts` 에서 `Object.values(floorAreas)`를 `Math.max` 에 전달할 때 발생하는 타입 불일치 에러를 해결하기 위해 `as number[]` 강제 캐스팅을 선언하여 타입 호환성을 확보하였습니다.

### 상세페이지 반응형 2열/1열 그리드 자동 전환 레이아웃 적용 (2026-06-14)

- **사용자 피드백 반영**: 사용자의 의견에 따라 고정 1열 배치 대신, 화면 폭에 대응해 2열과 1열을 자동으로 스위칭하고 그 안에서도 가변적으로 늘어나는 반응형 구조를 정립하였습니다.
- **상세 Drawer 가로폭 가변화**: 데스크톱 대화면 해상도(`xl`, `2xl`)일 때 상세 드로어(`detail-drawer`)의 너비를 가변적으로 확대(`xl:max-w-[1050px] 2xl:max-w-[1280px]`)하여 넓은 화면 공간을 효율적으로 채우도록 하였습니다.
- **자동 2열/1열 스위칭 그리드**: 상세페이지 탭 1 및 탭 2 내의 주요 요약 및 정보 카드 배치를 `grid-cols-1 xl:grid-cols-2`로 설정하여, 드로어 폭이 좁은 모바일이나 창 크기에서는 1열 스택으로 단정하게 표시되고 넓은 화면에서는 좌우 2열 그리드로 정렬되도록 반응형 흐름을 구현하였습니다.
- **모바일 웹 및 하이브리드 앱 검증**: 모바일 환경(`DetailScreen.tsx`)은 기본 1열 스택 구조의 반응형 여백 마진을 안전하게 유지하였습니다.
- **실서버 배포 및 E2E 자동화 검증**: `deploy.ps1`을 가동하여 Metro 번들링 후 Firebase Hosting(`<https://action-b8c75.web.app>`) 배포를 수행하였으며, 브라우저 서브에이전트 E2E 검증을 거쳐 해상도별(500px 및 1400px) 수직/수평 offset 좌표 렌더링이 정확하게 일치함을 검증 완료하였습니다.

### 검색 필터 예산 한도 최소값/최대값 동시 조정 기능 구현 및 z-index 버그 픽스 (2026-06-14)

- PC 웹(`index.html`)의 듀얼 레인지 슬라이더 겹침 영역 z-index 문제를 해결하기 위해, 슬라이더 컨테이너(`budget-slider-container`)에 마우스/터치 무브 이벤트를 리스닝하여 좌표 위치에 가장 가까운 슬라이더 핸들의 `z-index`를 동적으로 높여주는 스크립트를 적용하여 최대값과 최소값을 둘 다 완벽하게 조절할 수 있도록 개선하였습니다.
- 모바일 앱(`FeedScreen.tsx`)에서도 최소 예산 및 최대 예산 조절 칩 및 방어적 교차 연동 코드의 신뢰성을 재검토하고 완결성 있게 유지하였습니다.

### 상세페이지 2번 탭 미노출 오류 해결 및 3대 포털 로드뷰 연동 원복 (2026-06-19)

- **오타로 인한 DOM 계층 왜곡 수정**: `index.html` 내 `detail-group-panel-3` 하단에 발생한 오타(미완성 태그 `<`) 및 닫는 태그 누락으로 인해 `detail-group-panel-2`가 자식 노드로 종속되어 2번 탭 클릭 시 내용이 노출되지 않던 화이트아웃 결함을 수정하고 닫는 div 태그를 완벽 복구하였습니다.
- **내장 로드뷰의 3대 포털 아웃링크 일원화**: 1번 탭 상단에 불필요하게 재생성된 내장형 카카오 3D 로드뷰 영역을 완전히 걷어내고, 네이버/카카오/구글 3대 포털 로드뷰 비교 바로가기 링크를 보기 쉬운 카드 형태로 일체화 배치하여 1번 탭의 가독성과 연동 편의성을 극대화하였습니다. 기존 평면도 하단에 중복 노출되던 아웃링크 영역은 제거하였습니다.
- **추천 검색어 제안 복구**: 사용자가 빠른 탐색이 가능하도록 피드 목록 상단 영역에 "추천 검색어" 칩 바를 신설하고 클릭 시 검색창 인풋 바인딩 및 즉시 필터링이 가동되도록 처리하였습니다. 모바일 앱 `FeedScreen.tsx`에도 상응하는 가로 스크롤 형태의 추천 검색어 칩 바를 이식하여 사용자 경험의 PC-모바일 대칭성을 만족하도록 조치하였습니다.
- **모바일 상세화면 3대 탭 개편 및 내장 로드뷰 제거**: `DetailScreen.tsx` 내의 탭바를 3대 대분류(종합&권리분석, 입찰&금융분석, 입지&시세분석)로 축소 개편하였습니다. 기존 `rights`와 `etc_specs` 탭의 레이아웃 요소를 `general`(종합 & 권리분석) 탭 하단에 통합하여 사용자가 한 눈에 연동된 내용을 볼 수 있도록 개선하고, 내장형 카카오 로드뷰 WebView 컴포넌트를 완전히 삭제하여 3대 포털 아웃링크 버튼 카드로 일원화하였습니다.
- **E2E 배포 및 무결성 검증**: 수정 사항 반영 후 `deploy.ps1`을 가동하여 Expo 모바일 웹 빌드, 상대 경로 후처리 패치를 수행하고 Firebase Hosting(`<https://action-b8c75.web.app>`) 최종 배포 및 E2E 정상 연동을 완료하였습니다.

### 상세페이지 불필요한 빈칸 제거 및 실제 평면도 부재 시 평면도 숨김 결정 (2026-06-19)

- **공백 발생 원인 해결**: 1번 탭 상단에 카카오 내장 로드뷰 영역이 아웃링크 버튼 카드로 변경되면서, 버튼 영역 아래에 배치되었던 `detail-roadview-image-container`가 `hidden` 처리되었으나, 부모 그리드의 2열 반응형 레이아웃 및 우측 명세 카드 높이에 따라 카드가 늘어나 텅 빈 공백이 생기던 문제를 확인하였습니다.
- **단일 카드 통합 리팩토링**: 로드뷰 카드(`detail-roadview-card`)와 기본 명세 카드(`detail-spec-container`)를 `detail-panel-summary`라는 하나의 콤팩트한 1열 카드로 통폐합하여 공백이 생길 여지를 원천 차단하였습니다.
- **추정 평면도 강제 노출 중단**: 실제 매물 평면도(`item.images`가 없거나 빈 경우)가 없을 때, 면적 기반의 가짜 추정 평면도(59, 84, 114)와 가이드 카드를 억지로 노출하지 않고, 평면도 패널(`detail-panel-floorplan`)과 가이드 카드 자체를 `hidden` 처리하여 숨기도록 결정하였습니다.
- **모바일 앱 동일화**: `DetailScreen.tsx`에서도 실제 평면도 이미지가 존재하지 않는 경우 "📐 부동산 내부 평면도" 섹션 전체를 렌더링하지 않고 화면에서 숨김 처리하도록 대칭적으로 동일 구현을 진행합니다.
- **E2E 배포 검증**: 수정 후 `deploy.ps1`을 통한 무결성 빌드 및 Firebase 배포를 완수합니다.

### 상세페이지 2열 그리드 플로우 1열 해제 및 추정 면적 허수 제거 결정 (2026-06-19)

- **그리드 불균형 공백 해소**: 상세 Drawer 1번 탭 내에서 카드가 비대칭적으로 숨겨지거나(평면도 부재 등) 카드 간의 높이가 달라 발생하는 거대한 흰색 빈칸들을 근절하기 위해, 탭 내의 모든 `grid-cols-1 xl:grid-cols-2` 2열 배치를 해제하고 세로 1열 플로우 배치로 통일하기로 결정하였습니다. 이로써 빈칸이 원천 소멸하며 가로 너비가 확대되어 가독성이 극대화됩니다.
- **다세대/주택 다수 층 면적 합산 정밀 보정**: `parseAreasFromText` 함수가 세종 메종블룸 매물처럼 여러 층이 일괄 경매로 나온 다세대/주택 매물에 대해 개별 층 중 최댓값만 가져가던 한계를 수정하여, 다층 매물인 경우 층별 면적들의 전체 합산값(`totArea`)을 전용면적으로 올바르게 취득하도록 파싱 알고리즘을 보정하기로 결정하였습니다.
- **억지 허수 대입 로직 전면 소거**: 원본 데이터도 없고 텍스트 파싱도 실패한 면적 불명 매물에 대해 프론트엔드에서 59.9/84.9 등의 가짜 난수 면적(허수)을 임의로 대입해 오해를 주던 기존 폴백 로직을 전면 삭제하고 `0` 또는 `미상`으로 안전 노출하도록 변경하기로 결정하였습니다.
- **모바일 앱 하드코딩 폴백 제거**: `DetailScreen.tsx` 내에서 면적이 비어있을 경우 `84.9` 등으로 매핑하던 하드코딩 폴백을 제거하고, `0` 대입 시 자동으로 `미상`으로 노출되도록 동기화 처리하기로 하였습니다.

### 번지수 면적 오파싱 방지 및 가짜 단지 정보(학군, 건설사 등) 하드코딩 제거 (2026-06-19)

- **번지수 오파싱 방지**: 도로명 주소 번지수(예: 16)가 면적 단위 생략 파서의 숫자 유추 규칙(10~299)에 오작동 매치되어 "16평" -> "52.9㎡"로 잘못 계산되던 문제를 해결하기 위해, `mobile-app/src/utils/api.ts` 내의 `parseAreasFromText` 함수에서도 주소 부분을 제외한 텍스트로 한정하여 단위 생략 숫자 유추가 이루어지도록 정밀 보정하였습니다.
- **하드코딩 가짜 단지 정보 주입 차단**: 수집기가 수집하지 못한 단지 정보가 없는 매물에 대해 프론트엔드(`index.html` 및 `api.ts`)에서 임의로 "대치초등학교", "마이빌 오피스텔", "80세대" 등의 가짜 추정 데이터를 지어내 주입해 노출하던 폴백 로직을 전면 제거하였습니다. 실 데이터가 존재하지 않으면 명시적으로 `null` 처리하여 화면상에 대법원/온비드 경공매 단지 정보 미제공 안내가 출력되거나 모바일에서 비노출되도록 구조를 다듬어 데이터 무결성을 실현하였습니다.

### 어드민 페이지 롤백 버그 해결을 위한 Firebase Hosting cleanUrls 도입 결정 (2026-06-19)

- 사용자가 "어드민이 초기 버전으로 되돌아갔다"라고 느낀 근본 원인을 분석한 결과, Firebase Hosting의 SPA 리라이트 규칙(`** -> /index.html`)이 설정된 상태에서 사용자가 브라우저 주소창에 `/admin`을 입력해 접속할 때 실제 정적 파일인 `admin.html`로 매핑되지 못하고 `index.html`(홈 피드 메인화면)로 연결되어 초기화되는 현상을 식별하였습니다.
- 이를 영구적으로 해소하기 위해 `firebase.json` 설정에 `"cleanUrls": true` 옵션을 적용하여 확장자(.html)가 생략된 깔끔한 URL 접근을 정적 파일에 정상 매핑시켰습니다.
- 또한 리라이트 규칙 배열에 `/admin` 경로가 명확하게 `admin.html`을 목적지로 두도록 강제하는 라우팅 규칙을 추가하여 오작동 가능성을 원천 차단하였습니다.
- 이 설정을 로컬 빌드 및 Firebase 배포 프로세스(`deploy.ps1`)와 연계하여 최종 배포 검증을 추진하기로 하였습니다.

### v1.2 모의입찰, 전문가 연계 광고 및 어드민 관리 고도화 결정 (2026-06-19)

- **상세 Drawer 내 v1.2 피처 통합** 상세 Drawer 2번 탭(입찰 & 금융분석) 하단 영역에 실시간 가상 모의 입찰 센터 카드와 매수대리 전문가 연계 광고 카드를 이식하여 사용자가 매물 상세 보기 중에도 바로 모의입찰과 전문가 매칭을 연동 이용할 수 있도록 프론트엔드 컴포넌트를 긴밀히 결합합니다.
- **DOMContentLoaded 및 loadDetailView 실시간 연동** index.html 로드 시 v12Features.init() 및 v12ThemeCuration.injectCurationBar()를 연동하여 플로팅 AI 챗봇 오버레이 및 Curation Bar를 주입 가동하고, 매물 상세 열림 시 loadDetailView에서 v12Features.loadMockBids() 및 v12Features.renderExperts()를 호출해 Supabase 실시간 데이터를 UI와 완전 동기화합니다.
- **어드민 버전 v1.2 상향 및 관리 도구 확장** admin.html 헤더 버전 표기를 v1.2로 상향 갱신하고, 가입 회원 테이블에 membership_tier(일반/프리미엄) 관리 셀을 추가하며 변경 시 Supabase user_profiles와 동기화시킵니다. 또한 좌측 메뉴 및 우측 패널에 모의 입찰 매니저 탭을 신설해 mock_bids 데이터를 조회, 실시간 E2E 참여 현황을 추적하고 모니터링할 수 있는 체계를 완성합니다.

### v1.2 모바일 앱 오버레이 락 복구 및 결제 즉시 등업 연동 완수 (2026-06-19)

- **모바일 등급별 락 시스템 복구** 모바일 `DetailScreen.tsx` 파일 내 임시 오버레이 해제 조건(`false &&`)을 완전히 제거하여 회원 등급별(A/B/C) 상세 데이터 열람 락 시스템을 철저히 복구하였습니다.
- **즉시 프리미엄 결제 연동** 등업 상향 신청 버튼 터치 시 Supabase `user_profiles`를 실시간 갱신(`membership_tier='premium'`, `grade='A'`)하고 로컬 상태(`userGrade='A'`)를 동시에 업데이트하여 결제 즉시 등업 및 락 해제가 실시간 작동하도록 구현을 마쳤습니다.

### 전문가 상담 신청 서버 연동 및 어드민 통합 완료 (2026-06-19)

- **전문가 상담 신청 서버 연동** PC 웹 및 모바일 상세 화면 내 전문가 연계 광고 카드의 "온라인 상담 신청" 버튼을 클릭할 시 Supabase `expert_consultations` 테이블에 실시간으로 데이터가 입력되도록 서버 연동을 완료하였습니다.
- **어드민 전문가 상담 관리자 탭 신설** `admin.html`에 "전문가 상담 관리자" 탭 및 테이블을 신설하여 접수된 상담 내역을 실시간 모니터링하고 대기/완료 상태를 토글 처리하거나 기록을 삭제할 수 있는 서버 관리 체계를 통합 완료하였습니다.

### E2E 통합 빌드 및 Firebase 실서버 온라인 배포 (2026-06-19)

- **배포 및 무결성 확인** 수정사항이 반영된 Expo 모바일 웹 번들을 컴파일하고 `deploy.ps1`을 가동하여 Firebase Hosting 실서버에 최신 코드를 배포하였으며, 실서버에서의 정상 작동 여부를 E2E로 최종 확인 및 동기화하였습니다.

### 메인 큐레이션 및 상세 상하단 광고 고도화와 톤앤매너 폴리싱 (2026-06-19)

- **메인 큐레이션 위 광고 이동** PC 웹 메인 상단 배너 광고 슬롯의 위치를 테마 큐레이션 바 위로 최종 배포하여 순서를 교정하였습니다.
- **상세 상하단 고정형 광고판 이식** PC 웹 상세 Drawer 및 모바일 앱 `DetailScreen.tsx` 상세 화면에 상단 및 하단 고정형 광고판 슬롯을 추가하고 Supabase DB 연동 처리를 완료하였습니다.
- **여백 축소 및 톤앤매너 일치** 상세페이지 내 3대 탭(종합, 입찰, 입지) 내부의 여백과 카드 마진, 테이블 패딩을 약 20% 컴팩트하게 축소하고, 글자 폰트 서체 크기 등을 Royal Blue와 Slate HSL 톤앤매너로 통일하여 시각적 디자인 완성도를 극대화하였습니다.
- **무결성 배포 검증** 배포 자동화 프로세스를 기동해 최신 정적 빌드와 실서버 배포를 최종 완수하였습니다.

### 상세페이지 반응형 테이블 분할 및 미래 가치 예측 시스템 도입 결정 (2026-06-19)

- **테이블 가로 늘어짐 해소**: 대화면 데스크톱 해상도에서 상세 서랍 탭 1, 2, 3 내부의 테이블들이 너무 길게 늘어나는 가독성 저하 결함을 해결하기 위해 2열 그리드를 전면 적용하였습니다.
- **카드 항목별 디자인 펄 화이트 튜닝**: 각 카드 컴포넌트의 가독성을 높이기 위해 bg-slate-50 배경의 깔끔한 헤더 바와 border-b 구분선, 그리고 둥근 테두리 칸으로 세련되게 구획을 나누었습니다.
- **AI 미래 예상 시세 시뮬레이터 신설**: 물건의 감정가를 기준으로 보수적(연 1.5%), 표준(연 3.0%), 적극적(연 5.0%)인 연 복리 성장 시나리오를 설정하고, 1년, 3년, 5년, 10년 후의 추정 가격을 동적으로 계산하여 피드백하는 시뮬레이터 카드를 웹과 모바일에 대칭적으로 구현하여 사용자의 투자 예측 편의성을 제공하기로 결정하였습니다.
- **가상 시나리오 추정치 안내 및 고지**: 본 시뮬레이터의 예측 결과는 단순 복리 산식에 기반한 가상의 추정치에 해당하므로, 사용자 오해를 방지하기 위해 화면상에 주의 문구를 명시하고 사전 고지 처리를 추가하였습니다.

### 모바일 상세페이지 하단 잘림 복구 및 ScrollView 레이아웃 개선 결정 (2026-06-19)

- **하단 가림 현상 원인 규명**: 모바일 상세 화면(`DetailScreen.tsx`) 하단에 고정 광고 영역(`fixedAdContainerBottom`)이 추가됨에 따라, 전체 레이아웃을 감싸는 메인 `ScrollView`의 하단 콘텐츠(유사 추천 매물 등)가 광고 영역에 가려지고 스크롤이 끝까지 도달하지 못해 하단 부분이 잘려 보이지 않는 현상을 식별하였습니다.
- **ScrollView contentContainerStyle 보완**: 이를 근본적으로 해결하기 위해 메인 `ScrollView`에 `contentContainerStyle` 스타일을 새로 적용하고, 하단에 120px 가량의 충분한 여백(`paddingBottom: 120`)을 명시하여 스크롤 시 모든 하단 콘텐츠와 푸터가 가려짐 없이 원활하게 끝까지 올라오도록 레이아웃을 정밀 보정하였습니다.
- **스크롤 레퍼런스(ref) 매핑**: 또한 `DetailScreen.tsx` 내부에서 유사 추천 매물 등을 클릭하여 다른 매물 정보로 전환될 때 화면이 최상단으로 자동 스크롤되도록 선언만 되어 있고 매핑되지 않았던 `scrollViewRef` 레퍼런스를 `ScrollView` 컴포넌트의 `ref` 속성에 정상적으로 연결하여 사용성을 복구하였습니다.

### v1.2 프리미엄 고도화 보완 (미래 예상 시세 시뮬레이터 다차원화, 콤마 및 자금계획 스프레드 동기화, 어드민 Null 방어) (2026-06-19)

- **다차원 미래 시세 예측 모델 수립**: 단순 연 복리 상승률 방식에서 벗어나 감정가 기준 AI 점수, 입지 모멘텀(수도권/광역시 분기), 유찰 횟수당 거품 조정률, 거시 인플레이션 고정 상수(+1.2%)를 복합 연산하는 다차원 시뮬레이터 엔진을 웹과 모바일에 동일 적용하였습니다. 또한 "AI 분석 예측 모델 기준 (가상 시나리오 지표 - 허수)" 경고 문구를 화면에 명확히 표기했습니다.
- **모의입찰 금액 콤마 포맷터 및 자금 계획 스프레드 구현**: 모의입찰가와 스마트 계산기 입력창에 텍스트 타입 콤마 포맷터를 적용하여 가독성을 높였고, 모바일 상세 화면에도 입찰가 입력 시 취득세, 법무 대행비, LTV 대출한도(70%), 실소요자금, 필요 최소 자기자본을 산출하는 실시간 자금 계획 스프레드 뷰를 대칭 적용했습니다.
- **AI 7대 권리 리스크 및 투자 총평 법률 근거 보강**: 리스크 리포트와 투자 분석 총평의 설명력을 민법(제320조, 제366조, 제265조, 제213조 등) 및 주택임대차보호법(제3조 등) 법조문과 대법원 판례, 강제집행 및 이사비 합의 실무 가이드라인을 근거로 분량을 2배 이상 상세화했습니다.
- **어드민 DB 조인 Null 방어**: `admin.html`에서 전문가 상담 목록 로드 시 빈 ID 목록이 전달되는 상황을 사전에 `filter(Boolean)`로 정제하여 Null 조인 에러가 발생하지 않도록 방어 로직을 보충했습니다.
- **통합 빌드 배포 완수**: `deploy.ps1`을 실행하여 Expo 웹 모바일 컴파일 및 Firebase Hosting 실서버 무결성 최종 배포 및 브라우저 E2E 검증을 마쳤습니다.

### v1.2 전문가 광고, 모의입찰, 광고관리자, 회원관리, 로드뷰 연동 보완 및 고도화 결정 (2026-06-19)

- **모바일 전문가 연동 실시간화**: 기존 모바일 앱 상세화면에 하드코딩되어 있던 두 전문가 정보를 Supabase 'experts' 테이블에서 실시간 조회하여 렌더링하는 dynamic flow로 개편하였습니다. 테이블 로드 실패 시에는 기존 전문가 데이터를 예비용(fallback)으로 바인딩하여 안정성을 확보하였습니다.
- **모의입찰 가격 정보 보강**: 웹과 모바일 모의입찰 입력 필드 바로 위에 해당 매물의 감정가 및 최저매각가를 천단위 쉼표 포맷으로 표기하여, 가상 입찰가격을 작성할 때 직관적으로 참고할 수 있도록 하였습니다.
- **정밀 타겟 광고 구좌 관리자 및 미니맵 고도화**: 어드민의 UI 1:1 대칭 배치를 고강도 개편하여 시각적 균형을 수립하고, 광고 가이드를 대폭 상세화하였습니다. 또한 레이아웃 미니맵의 가로폭 최대치를 키우고 상세페이지 화면 모형을 추가 탑재하여 detail_top_banner와 detail_bottom_banner 구좌의 위치도 확실히 알 수 있게 1.5배 스케일로 고도화하였습니다.
- **회원 및 등급 관리 시스템 고도화**: 회원 목록 테이블에 고유한 일련번호 No. 컬럼이 검색이나 정렬 결과에 상관없이 정합성을 유지하도록 구현하였습니다. 가입 회원 상세 모달에 해당 회원이 참여한 모의입찰 횟수 및 전문가 상담 신청 건수를 Supabase에서 실시간 집계하여 표시해주는 지능형 통계 기능을 탑재하였습니다.
- **네이버 지도 로드뷰 연동 주소 체계 수정**: 네이버 지도의 새로운 규격에 대응하여 PC 웹 아웃링크는 'map.naver.com/p/search'로, 모바일 앱 아웃링크는 주소 검색이 정확한 'm.map.naver.com/search.naver'로 전면 교정하여 404 오류를 차단하였습니다.

### 루트 도메인(https://myauction.r-e.kr/) 일괄 교체 및 임시 도메인 참조 제거 결정 (2026-06-19)

- **www 서브도메인 제거 및 루트 도메인 단일화**: 사용자의 요청에 따라 기존에 혼용되던 www.myauction.r-e.kr 도메인을 www가 없는 루트 도메인인 myauction.r-e.kr로 완전히 변경하기로 결정하였습니다.
- **임시 Firebase 호스팅 도메인 참조 완벽 삭제**: 소스코드에 남아있을 수 있는 구 Firebase 호스팅 도메인(action-b8c75.web.app) 관련 모든 텍스트 및 링크를 제거하고 루트 도메인으로 대체하였습니다.
- **PC 및 모바일 소스코드 정합성 유지**: 웹용 index.html, admin.html, policy.html 파일뿐만 아니라, 모바일 앱용 AuthScreen.tsx, FeedScreen.tsx, GuideScreen.tsx 소스코드 내의 OAuth 리다이렉트 주소와 광고 랜딩 주소도 루트 도메인 및 모바일 주소 규칙에 맞춰 전량 치환하기로 결정하였습니다.

### 로그인 전후 공식 도메인 강제 리다이렉트 및 어드민 저장소 정교화 결정 (2026-06-19)

- **모바일 웹 빌드본 리다이렉트 스크립트 자동 주입**: Expo 웹 빌드 후처리 시 동작하는 `relative_path_patch.py` 스크립트를 보강하여, 생성된 모바일 index.html의 head 태그 시작 부분에 구 임시 도메인(`action-b8c75.web.app`) 접속 시 공식 도메인(`myauction.r-e.kr`)으로 강제 이동시키는 JS 코드를 자동 주입하도록 설정하였습니다. 이로써 모바일 웹 환경에서도 구 도메인으로 복귀하거나 잔류하는 모든 경로를 원천 차단하였습니다.
- **어드민 광고 저장 단계 도메인 사전 치환**: 어드민 화면(`admin.html`)에서 광고 배너 설정을 최종 저장할 때, 인풋 창에 구 임시 도메인이 기입되어 있더라도 데이터베이스에 반영되기 전에 강제로 공식 도메인 주소로 치환하여 upsert하도록 `saveAdSettings` 로직을 정밀 보정하였습니다.
- **시드 데이터 정정**: 초기 데이터 주입용 `insert_detail_ads.py` 스크립트 내부의 하드코딩된 구 도메인 링크들을 모두 공식 도메인으로 일괄 정정 완료하였습니다.
- **브라우저 캐시 무력화 헤더 추가**: `firebase.json` 설정 파일에 HTML 파일에 대한 캐시 차단(`Cache-Control: no-cache, no-store, must-revalidate`) 헤더를 탑재하여, 사용자가 구 버전의 HTML 캐시를 사용하여 리다이렉트 스크립트를 우회하지 못하도록 배포 조치했습니다.
- **Supabase Auth 서버 설정 조율**: 소셜 로그인 시도 후 화이트리스트 검증을 거치는 Supabase 서버 측 리다이렉션 특성을 분석하고, 사용자가 콘솔에서 Site URL을 새 도메인으로 변경하도록 안내 지침을 마련했습니다.

### Supabase 프로젝트명 변경 및 루트 도메인 단일화 완수 결정 (2026-06-19)

- **Supabase 콘솔 프로젝트명 변경**: 사용자의 요청에 대응하여 Supabase 웹 대시보드 상의 프로젝트 표시명(Display Name)을 기존 "Action"에서 공식 도메인과 부합하는 "myauction"으로 변경 완료하였습니다.
- **www 제외 루트 도메인(myauction.r-e.kr) 전면 단일화**: 리다이렉트 주소 및 기본 광고 링크, 트러블슈팅 안내 가이드 텍스트 등 전 영역에 걸쳐 사용되었던 `www.myauction.r-e.kr` 문자열들을 `myauction.r-e.kr`로 정밀 정정 완료하였습니다.
- **빌드 후처리 자동 치환 보정**: `relative_path_patch.py`에서 모바일 인덱스 파일 빌드 시 동적 주입되는 스크립트 또한 `myauction.r-e.kr`로 치환되도록 수정하여 최종 빌드 무결성을 확립하였습니다.

### GitHub, Firebase, 모바일 앱 설정명 myauction 일관 통일 결정 (2026-06-19)

- **모바일 앱 식별자 및 이름 정정**: `app.json` 설정 파일 내 앱 타이틀(`name`)을 공식 표기인 `"부동산경공매 검색시스템"`으로 정정하고, Expo 식별자(`slug`)를 `"myauction"`으로 수정하여 프로젝트 정체성을 일치시켰습니다. 또한 `package.json` 내 name도 `"myauction"`으로 정밀 수정 완료하였습니다.
- **Git 원격 저장소 URL 갱신**: 사용자가 GitHub 웹 콘솔에서 저장소명을 `myauction`으로 변경한 후, 로컬 git 설정의 remote origin URL을 `https://github.com/hl1oex/myauction.git`으로 갱신하여 원격 동기화가 유지되도록 복구하였습니다.
- **최종 실서버 통합 배포**: Expo 빌드를 전면 재수행하여 신규 앱 메타데이터 및 도메인 동기화가 이루어진 JS 번들셋을 Firebase Hosting 실서버에 E2E 릴리즈 완료하였습니다.

### 미사용 Firebase 프로젝트 정리 결정 (2026-06-20)

- **운영/미사용 프로젝트 식별**: 현재 Firebase Hosting 배포와 Firestore 실시간 연결에 사용되는 유일한 운영 프로젝트가 `action-b8c75` (myauction)임을 CLI 진단으로 입증하였습니다.
- **폐기 완료**: 사용하지 않고 방치되어 있던 레거시 프로젝트 2개(`myauction-759d7` 및 `priceit-app-5eada`)에 대해, 사용자가 Firebase 웹 콘솔에서 직접 종료(Shut down) 처리를 함으로써 잠재적 보안 리스크와 혼선을 완전히 차단하였습니다.

### 원형 내부 비움 및 투명 배경 파비콘 이식 결정 (2026-06-20)

- **디자인 컨셉 조합 결정**: 사용자의 피드백을 수용하여 컨셉 1의 "방패" 문양 내부에 컨셉 2의 "집과 입찰봉" 실루엣이 결합된 원형 은색/회색 톤의 아이콘 이미지를 새로 생성하였습니다.
- **로컬 스크립트 기반 배경 투명화 가공**: AI 이미지의 한계로 불투명하게 채워져 생성된 검은색 외곽 및 내부 배경을 완전 투명(Alpha=0)하게 가공하기 위해 `make_favicon_transparent.py` 후처리 스크립트를 작성하여 가동하였습니다.
- **E2E 이식 및 실서버 배포**: 투명 가공이 완료된 `favicon.png`를 루트 폴더 및 모바일 앱 에셋 폴더로 복사 이식하고, `deploy.ps1`을 가동하여 Firebase Hosting에 통합 동기화 배포를 완수하였습니다.

### 모바일 앱 TypeScript 컴파일 오류 해결 결정 (2026-06-20)

- **타입 정의 누락 해결**: 신규 기능 개발 과정에서 타입 파일에 갱신되지 않고 누락된 `Property`의 `ai_grade`, `subway_info`, `title` 속성과 `FilterState`의 `selectedDongs` 속성을 `mobile-app/src/types/index.ts`에 추가하여 컴파일 에러를 해결하기로 결정하였습니다.
- **인라인 스타일 속성 교정**: `DetailScreen.tsx`에서 React Native에서 지원하지 않는 단축 스타일명인 `pb: 4`를 표준 React Native 스타일 명세에 맞춰 `paddingBottom: 4`로 수정하였습니다.
- **암시적 any 타입 명시**: `FeedScreen.tsx` 내의 Array helper 메서드(`some`, `filter`) 콜백 함수들의 인자 타입이 암시적 `any`로 유추되어 발생하는 빌드 오류를 방지하기 위해 타입을 명시적으로 선언하도록 하였습니다.

### 슬랙 및 텔레그램 공유 기능 고도화 및 모바일 UI 최적화 결정 (2026-06-20)
- **모바일 상세 화면 공유 UI 이관**: 헤더에 임시로 좁게 우겨넣었던 이모지 공유 버튼(`💬`, `✈️`)들을 걷어내고, 매물 요약 정보 카드(`summaryCard`) 하단에 가로로 넓게 펄 화이트 디자인에 걸맞은 프리미엄 버튼 세트로 개편하여 시각적 완성도와 사용성을 모두 극대화하기로 결정하였습니다.
- **텔레그램 Chat ID 입력용 커스텀 모달 구현**: 모바일 네이티브 환경(안드로이드/iOS)에서는 브라우저 고유의 `window.prompt` 대화창이 존재하지 않아 사용자가 Chat ID를 직접 기입하지 못하고 실패하던 원천 한계를 극복하기 위해, React Native의 `Modal`, `TextInput`을 조합한 세련된 모달식 Chat ID 입력 및 로컬 상태 관리 장치를 `DetailScreen.tsx` 내부에 자체 탑재하기로 결정하였습니다.

### 텔레그램 Chat ID(426045277) 하드코딩 고정 및 대화창 제거 핫픽스 결정 (2026-06-20)
- **Chat ID 조회 및 고정**: 텔레그램 봇 API `getUpdates` 수집 정보를 분석하여 사용자의 고유 Telegram Chat ID가 `426045277` 임을 최종 입증하여 코드 내에 상수로 전격 고정하였습니다.
- **불필요한 팝업(Alert/Prompt/Modal) 전면 제거**: PC 웹(`index.js`)과 모바일 앱(`DetailScreen.tsx`)에서 텔레그램 공유 시 띄우던 prompt 다이얼로그와 입력 모달창을 모두 완전 삭제하여, 공유 버튼 한 번의 클릭만으로 팝업 없이 사용자 본인의 텔레그램 방으로 즉시 1초 전송이 완료되는 스마트 E2E UX를 완성하였습니다.

### 슬랙 연동 CORS 프록시 및 3단계 인증 방식 폴백 적용 결정 (2026-06-20)
- **CORS 우회 통신로 안전성 강화**: `corsproxy.io` 단일 우회 시 발생할 수 있는 브라우저 프리플라이트 OPTIONS 요청 거부 결함을 방어하기 위해 `allorigins.win`, `thingproxy.freeboard.io`를 순차 대입하는 프록시 폴백 헬퍼(`fetchViaProxy`)를 도입했습니다.
- **슬랙 인증 방식 다변화 대응**: 쿼리 파라미터 기반 토큰 전달이 보안 강화 정책으로 거부될 경우를 고려하여, Authorization Bearer 헤더 방식, POST body urlencoded 토큰 방식, 기존 쿼리 파라미터 토큰 방식을 3단계로 유연하게 순차 실행하는 우회 체인을 구축하고 PC와 모바일 전체에 포팅 완료했습니다.

